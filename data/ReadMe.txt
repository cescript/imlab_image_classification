Classification example reads the data in the train folder and train a classifier for each sub folder. After that it scans all the images in the test folder and categorize them into the result folder.

All images must be 24 bit bmp file with the same size.
Image size should be defined in the example c file.